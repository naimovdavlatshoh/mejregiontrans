// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: {
        unoptimized: true, // Tasvir optimallashtirishni o‘chirib qo‘yish
    },
};

module.exports = nextConfig;
